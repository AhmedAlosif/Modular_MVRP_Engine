def solve_vrp(data):
    # Convert to OR-Tools format
    # Solve with constraints
    # Return list of routes
    return {
        "routes": [
            {"vehicle_id": "v1", "path": ["depot", "a", "b"], "distance_km": 12.3}
        ]
    }
